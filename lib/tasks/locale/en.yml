en:
  docker:
    install: install Docker
  git:
    install: install Git with git-lfs extention
  god:
    install: install God
  jenkins:
    install: install Jenkins
    plugins:
      install: install Jenkins plugins
  letsencrypt:
    install: install Let's Eencrypt certbot
  memcached:
    install: install mecached
  mysql:
    install: install MySQL/MariaDB
  nginx:
    config:
      default: setting Passenger for app to Nginx
      gitbucket: setting GitBucket as reverse proxy
      nginx: setting Jenkins as reverse proxy
    install: install Nginx
  obs_studio:
    install: install OBS-Studio
  redis:
    install: install Redis
  trac:
    install: install Trac
  unicorn:
    install: install application as unicorn service
  vsftpd:
    install: install vsftpd
