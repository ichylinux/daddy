en:
  itamae:
    errors:
      unsupported_os_version: unsupported os version. %{os_version}
    messages:
      jenkins:
        after_install: |
          installation completed.
          please access %{jenkins_url} and add first user.
          use public key below for your git repository.
          
          %{public_key}
