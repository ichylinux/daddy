<h2>ようこそ！</h2>

<%= render 'search' %>

<% if @careers.present? %>
  <table id="careers_table">
    <tr>
      <th><%= User.human_attribute_name :full_name %></th>
      <th><%= User.human_attribute_name :birthday %></th>
      <th><%= User.human_attribute_name :gender %></th>
    </tr>

  <% @careers.each do |career| %>
    <tr>
      <td><%= career.user.full_name %></td>
      <td><%= career.user.birthday %></td>
      <td><%= career.user.gender_name %></td>
    </tr>
  <% end %>
  </table>
<% end %>